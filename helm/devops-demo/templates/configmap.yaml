apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devops-demo.fullname" . }}-config
  labels:
    {{- include "devops-demo.labels" . | nindent 4 }}
data:
  # Application configuration
  APP_NAME: "DevOps Demo API"
  LOG_LEVEL: "INFO"
